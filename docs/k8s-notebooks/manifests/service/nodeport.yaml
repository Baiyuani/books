apiVersion: v1
kind: Service
metadata:
  labels:
    app: example
  name: example-nodeport
#  namespace: app1
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30080
  selector:
    app: example
  sessionAffinity: None
  type: NodePort