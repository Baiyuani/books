apiVersion: v1
kind: Service
metadata:
  labels:
    app: example
  name: example-headless
#  namespace: app1
spec:
  clusterIP: None   #使用该字段让service不再通过虚拟IP来负载均衡
  ports: 
  - protocol: TCP 
    port: 80
    targetPort: 80
  selector:
    app: example
